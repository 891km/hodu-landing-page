# 1번 리뷰

- 범위 : styles/main.css | 5 ~ 20 line (:root 부분)

- 전체 개요
  CSS 변수로 색상, 패딩, 폰트 크기, 카드 스타일 등 기본 스타일 값들을 정의하였습니다.

- 기능 내용

  1. `--padding`은 화면을 키웠을 때 컨텐츠의 너비가 1280px를 넘지 않고, 최소 4rem로 유지하게 위해 `max(4rem, calc((100% - 1280px) / 2))`로 작성하였습니다.
  2. `-fs-heading-md-fluid`에는 `clamp(x, y, z)`를 이용하여 x에는 모바일 버전의 heading 사이즈와, z에는 데스크톱 버전의 사이즈를 넣었습니다. y값에는 개발자 도구에서 사이즈를 봐가면서 적절해 보이는 값으로 설정했습니다.

- 기타 (질문)

  1. padding에 `max()` 함수를 사용하는 방식이 적절한지, 제 의도대로 잘 작성한건지 궁금합니다.
  2. `clamp()` 함수의 중간값(y) 설정 기준과 적절한 값을 찾는 방법에 대해 조언 부탁드립니다!

# 2번 리뷰

- 범위 : styles/main.css | 80 ~ 87 line (.text-container 부분)

- 전체 개요
  공통 클래스를 부여해 반복되는 ‘제목-설명’ 구조에 기본 스타일을 적용하고, 섹션별 개별 스타일은 선택자를 사용하여 덮어썼습니다.

- 기능 내용

  1. `.text-container`로 기본 틀을 설정하였습니다.
  2. 섹션별 차이를 위해 `.section-banner .text-container {...}` 같은 형태의 선택자를 사용하여 개별 스타일을 추가 및 변경하였습니다.

- 기타 (질문)

  1. 이런 방식의 선택자 중첩 사용이 적절한지
  2. 선택자 범위가 깊어지지 않게 하는 더 나은 방법이 있는지 궁금합니다.

# 3번 리뷰

- 범위 :
  styles/main.css | 365 ~ 415 line,
  styles/mobile.css | 209 ~ 224 line
  (.article-subscribe form 부분)

- 전체 개요
  `.article-subscribe form` 영역에서 이메일 입력과 ‘subscribe’ 버튼 레이아웃을 구성하였습니다.

- 기능 내용

  1. 데스크톱 화면에서는 `form`에 `display: flex`를 주어 한 줄로 배치하였고, `.input-wrapper`와 `::before`를 이용하여 별도의 태그 없이 이메일 아이콘을 추가하였습니다.
  2. `form`에 `  flex-direction: column;`으로 수정하고 기존 `form`에 적용했던 배경 스타일링을 `.input-wrapper`로 옮겨 버튼이 분리되는 것처럼 보이게 하였습니다.

- 기타 (질문)

  1. 현재 방식이 시각적으로는 잘 작동하나, 코드의 가독성과 정렬이 깔끔하지 않은 느낌이 들어 더 효율적이거나 단순한 구현 방법이 있는지 질문드립니다.

# 4번 리뷰

- 범위 : styles/mobile.css | 255 ~ 295 line (footer 부분)

- 전체 개요
  `footer` 레이아웃을 `flex`와 `position` 속성을 조합하여 구성하였습니다.

- 기능 내용

  1. `footer`에 `flex`를 적용하였습니다.
  2. `.footer-links`에 `position: absolute` 적용해 flex와 별도로 오른쪽에 위치하도록 하였습니다.

- 기타 (질문)
  1. flex와 absolute 혼합이 적절한지
  2. grid 레이아웃이나 다른 구조가 더 적합한지 궁금합니다.

# 5번 리뷰

- 범위 : index.html | 131 ~ 158 line (article 태그 부분)

- 전체 개요
  처음에 `aside` 태그로 작성한 부분을 `article` 태그로 수정하였습니다.

- 기능 내용

  1. 재사용할 수 있는 콘텐츠라고 판단해서 `article`로 변경하였습니다.

- 기타 (질문)

  1. `aside` 태그에서 `article` 태그로 수정했는데 `article` 태그의 사용이 적절한지, 어떤 태그가 적절한지 알고 싶습니다.

# 6번 질문

- 범위 : index.html | 20 ~ 44 line (header 태그 부분)

- 전체 개요
  사이트의 상단 `header` 영역에 네비게이션과 로고를 배치하고, `aria-label`을 작성하여 접근성을 고려했습니다.

- 기능 내용
  스크린 리더 사용자 접근성 강화를 위해 `a, button` 태그에 `aria-label` 속성을 추가하였습니다.

- 기타 (질문)

  1. aria-label 작성 방식이 적절한지
  2. 웹 접근성 측면에서 추가로 고려해야 할 사항이 있는지 확인 받고 싶습니다.

# 7번 리뷰

- 범위: CSS 파일 구조 전반

- 전체 개요
  현재 모든 스타일을 main.css에 작성하고, 주석으로 구분하여 관리하고 있습니다.

- 기능 내용

  1. main.css 내에서 영역와 주요 기능을 주석으로 구분했습니다.

  -기타 (질문)

  1. 파일 자체를 더 분리해야 하는지
  2. 분리를 해야 한다면, 분리 기준과 적정 파일 개수 등이 궁금합니다.
